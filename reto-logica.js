/* Retos de logica*/ 
/* 1- Dado un array de enteros nums y un entero target, de vuelve los indices
de los dos numeros que suman el valor del target.

Puedes asumir que cada entrada tendra exactamente una solucion y no puedes
usar el mismo elemento dos veces. 

Puedes devolver la respuesta en cualquier orden.

Ejemplos:

Entrada: nums = [2,7,11,15], target = 9
salida: [0,1]
Explicacion: Dado que nums[0] + nums[1] == 9,
devolvemos [0,1].

Entrada: nums = [3,2,4], target = 6
salida: [1,2]*/